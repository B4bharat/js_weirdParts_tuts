/*
	Operators are functions which are called syntactically differently than one would normally calls functions. it usually takes in two parameters.

	var result = 3 + 4;
	Here plus(+) is an operator and it has two paramters 3 and 4.
	It is called an 'infix' operator because its in between two paramters.
	+3 4 would've been 'prefix' operator
	3 4+ would've been 'postfix' operator
*/
